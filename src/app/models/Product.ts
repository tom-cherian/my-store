export interface Product {
    id: number;
    title: string;
    imageUrl: string;
    description: string;
    amount: number;
    isAvailable: boolean;
    updatedAt: string;
    createdAt: string;
}
